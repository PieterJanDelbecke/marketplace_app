<h1> Item show page</h1>

<div class="jumbotron">
    <div class="row">
        <div class="row-md-6">
            <h1 class="display-4"><%= @item.name %></h1>
            <p class="lead"> <%= @item.description %>
            <hr class="my-4">
            <p class="lead">
                <%= button_to 'Order', new_booking_path, method: :get, params: {current_item: @item.id}, class:"btn btn-primary btn-lg" %>

        </div>
        <div class="cal-md-6">
            <% if @item.picture.attached?%>
                <%= image_tag @item.picture , class:"item-show-image"%>
            <% end %>
        </div>
    <div>
</div>










<% if false %>

if @item.picture.attached?

<div class="row">
    <br><br>
    <%= image_tag @item.picture if @item.picture.attached?%>
     <ul>
        <li> Name: <%= @item[:name] %> </li>
        <li> Description: <%= @item[:description] %> <li>
        <li> Price: <%= @item[:price] %> <li>
        <li> new: <%= @item[:new] %> <li>
        <li> sold: <%= @item[:sold] %> <li>
    </ul>
    <br> 
</div>
<%= link_to 'Back', :back %>
<% if policy(@item).update? %>
    <%= link_to 'Edit', edit_item_path(@item) %>
    <%= link_to 'Destroy', @item, method: :delete, data: { confirm: 'Are you sure?' } %>
<% else %>
    <%# <%= button_to 'Order', bookings_path, method: :post, params: {current_item: @item.id} %> %>
    <%= button_to 'Order', new_booking_path, method: :get, params: {current_item: @item.id} %>

<% end %>
<% end %>
