<%= simple_form_for(@booking) do |f| %>

  <div class="form-inputs">
    <%= f.association :item %>
    <%= f.association :user %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
  
<% end %>