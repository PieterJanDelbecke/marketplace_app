<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
            <%= link_to "BIKEPACKER", root_path , class:"nav-link active" %>
        </li>
        <li class="nav-item">
            <% if user_signed_in? %>
            <%= link_to "Sign Out", destroy_user_session_path, method: "delete", class:"nav-link" %>
            <% else %>
            <%= link_to "Sign In", new_user_session_path, class:"nav-link active" %>
            <% end %>
        </li>
        <li class="nav-item">
            <% if !user_signed_in? %>
            <%= link_to "Sign Up", new_user_registration_path, class:"nav-link" %>
            <% end %>
        </li>
        <li class="nav-item">
            <%= link_to "Sell Bike", new_item_path, class:"nav-link" %>
        </li>
        <li class="navbar-text"><%= current_user.username if user_signed_in? %></li>
      </ul>
    </div>
  </div>
</nav>